## SSL issues when connecting to `gitlab.ucls.uchicago.edu`

- Tends to happen with Microsoft computers
- Create a local ssl cert bypass
    - Run the command `git config --local http.sslVerify "false"`
