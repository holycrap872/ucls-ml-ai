1. Match each of the following code snippets with their type of data structure:

Left:
- x = [1, 2, 4]
- x = ["George", "Thomas", "John"]
- x = {1, 2, 3, 4}
- x = {"hermione", "bill", "sheila", "ted"}
- x = {"taylor": 2610, "beyonce": 407, "sabrina": 362}
- x = {0: 1, 1: 1, 2: 2, 3: 3, 4: 5, 5: 8}

Right:
- list[str]
- set[str]
- set[int]
- dict[str, int]
- list[int]
- dict[int, int]

2. Which data structure would best represent the following situation: The order that students enter a classroom.
- list[int]
- list[str]
- set[int]
- set[str]
- dict[str, int]
- dict[str, str]

3. Which data structure would best represent the following situation: The grade of each student in a class.
- Same options as above

4. Which data structure would best represent the following situation: The name of everyone's favorite restaurant.
- Same options as above

5. Which data structure would best represent the following situation: How often each student has been to class.
- Same options as above

6. Which data structure would best represent the following situation: The name of your siblings.
- Same options as above

7. Why can you create a set with elements in it using curly brackets (e.g., `x = {1, 2, 3}` ) but you can't create and empty set using curly brackets (e.g., `x = {}` )
- The creation of any data structure requires at least one element
- All sets must be composed of integers
- The brackets need to have a space between them
- Any empty set would be confused with an empty dictionary

8. What are the names of the two pieces in a dictionary?
- {"key": "value"}
- {"lock": "smith"}
- {"question": "answer"}
- {"start": "end"}
